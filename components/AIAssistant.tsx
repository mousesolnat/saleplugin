import React from 'react';

// This component has been disabled as per request to remove AI features.
// It returns null to ensure no rendering if imported.
export const AIAssistant = () => {
  return null;
};
